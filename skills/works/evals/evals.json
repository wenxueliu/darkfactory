{
  "skill_name": "works",
  "runtime": "OpenCode",
  "model": "MiniMax M2.7",
  "evals": [
    {
      "id": 1,
      "prompt": "/works。根据根目录 requirement.md 全自动完成，不要询问我。",
      "assertions": [
        "works 保持唯一流程控制；审查使用全新只读 subagent，subagent 返回 payload 而 Works 负责落盘",
        "contract-init 后由全新只读 contract-author subagent 返回完整 payload，Works 负责校验和写入",
        "contract-author 已注册为 OpenCode subagent，主 Agent 通过 Task 调用并等待非空 task result 后才运行 contract-check",
        "requirement-contract 覆盖文档中的全部独立行为",
        "每个 Req 都有 implementation、test 和 replay 证据",
        "契约验收和独立实现审查都通过后才进入 COMPLETE"
      ]
    },
    {
      "id": 2,
      "prompt": "/works。三个 Req 位于多模块 Maven 项目，请无人值守实现，只运行对应新行为的定向 Mockito 测试。",
      "assertions": [
        "契约保存三个有序 Req",
        "验收命令选择正确受影响模块，但只执行当前 Req 的 Class#method",
        "每个 Req 测试 checkpoint 后自动进入下一 Req",
        "中途没有请求人工确认"
      ]
    },
    {
      "id": 3,
      "prompt": "/works。第一次新测试会因为 fixture 配置错误而失败，请自行诊断直到完成。",
      "assertions": [
        "fixture 或编译错误不生成有效 test 证据",
        "模型依据输出修正后重新运行",
        "目标 testcase 修复后必须真实执行并通过",
        "最终 finalize 通过"
      ]
    },
    {
      "id": 4,
      "prompt": "/works。需求入口为 RestController，项目已有对应 Service API，请全自动实现。",
      "assertions": [
        "impact-map 记录入口、Service API、持久层和测试 seam",
        "Controller 复用或扩展 Service API",
        "Service 实现先复用本类已有等价方法，不为相同能力重复调用 Mapper",
        "Service boundary verify 通过",
        "行为验收命令通过"
      ]
    },
    {
      "id": 5,
      "prompt": "/works。请完成 requirement.md；只测试本次实现行为，使用 Mockito，不要使用 SpringBootTest，第三方依赖直接 mock。",
      "assertions": [
        "每条验收命令使用唯一且精确的 -Dtest=Class#method，不运行存量测试",
        "测试使用 Mockito 且不含 SpringBootTest，第三方依赖全部直接 mock",
        "final-verification 逐条记录契约命令和退出码"
      ]
    },
    {
      "id": 6,
      "prompt": "/works。长任务包含四个行为，持续执行到全部完成。",
      "assertions": [
        "每次 status 只有一个 next_action",
        "四个行为均映射到 Req 和验收标准",
        "任务后半段仍保持相同 code-first 证据与验收范围",
        "没有阶段性总结后停止"
      ]
    },
    {
      "id": 7,
      "prompt": "/works。探索、实现后测试、调试和验证使用内置 reference，审查使用 impl-validator subagent，但不要询问我。",
      "assertions": [
        "内置 reference 或 impl-validator 只按当前阶段使用，works state 仍是唯一流程状态",
        "内置 reference 不要求人工确认，全程无用户提问",
        "reference 的结论不能替代 implementation、test 或 finalize 证据",
        "开发 subagent 不控制状态，两个审查使用全新只读 subagent，审查 JSON 由 Works 主 agent 写入"
      ]
    },
    {
      "id": 8,
      "prompt": "/works。任务很长，可能发生上下文压缩；失败时不要重复无效动作。",
      "assertions": [
        "status 返回包含 id、req、skill、subagent、reference 和 success_evidence 的结构化 next_action",
        "失败动作写入 activity.jsonl 和 state attempts",
        "相同命令与工作区的失败重放被 E901 阻止",
        "recover 返回最后活动、最后失败和当前 next_action",
        "关键发现与决策可写入独立 JSONL，且不成为第二套流程状态",
        "关键阶段生成简短 summaries"
      ]
    },
    {
      "id": 9,
      "prompt": "/works。需求拆分和最终实现都必须由独立上下文复核，失败时自动修复。",
      "assertions": [
        "contract-check 后进入 CONTRACT_REVIEW_REQUIRED",
        "契约审查失败时修订契约并启动新的 verifier",
        "finalize 后进入 IMPLEMENTATION_REVIEW_REQUIRED",
        "实现审查失败时 reopen 对应 Req 并重新完成 implementation/test/finalize",
        "只有独立实现审查 PASS 才进入 COMPLETE"
      ]
    }
  ]
}
